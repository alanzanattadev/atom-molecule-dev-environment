import React from 'react';
import { shallow, mount } from 'enzyme';
import DiagnosticsSelectors from './DiagnosticsSelectors';

describe('DiagnosticsSelectors', () => {
  it('should display warnings and errors when there is no infos and successes', () => {
    let subject = ;


  });

  it('should display infos and successes when there is no warnings and errors', () => {
    let subject = ;


  });

  it('should display diagnostics mode selector', () => {
    let subject = ;


  });

  it('should display diagnostics search bar', () => {
    let subject = ;


  });

  it('should call onBack with type on diagnostics selector back arrow click', () => {
    let subject = ;


  });

  it('should call onForward with type on diagnostics selector forward arrow click', () => {
    let subject = ;


  });

  it('should call onModeChange with new mode on diagnostics mode selector click', () => {
    let subject = ;


  });

  it('should call onSearchChange with search on diagnostic search bar change', () => {
    let subject = ;


  });
});
