import React from 'react';
import { shallow, mount } from 'enzyme';
import DiagnosticsTraveller from './DiagnosticsTraveller';

describe('DiagnosticsTraveller', () => {
  it('should display diagnostics selectors', () => {
    let subject = ;


  });

  it('should display diagnostics details', () => {
    let subject = ;


  });

  it('should display warnings details when warning selector is clicked', () => {
    let subject = ;


  });

  it('should display infos details when infos selector is clicked', () => {
    let subject = ;


  });

  it('should display errors details when errors selector is clicked', () => {
    let subject = ;


  });

  it('should display successes details when successes selector is clicked', () => {
    let subject = ;


  });

  it('should change details to display when a back arrow is clicked', () => {
    let subject = ;


  });

  it('should change details to display when a forward arrow is clicked', () => {
    let subject = ;

    
  });

  it('should display every details when log mode is selected', () => {
    let subject = ;


  });

  it('should filter with the search', () => {
    let subject = ;


  });
});
