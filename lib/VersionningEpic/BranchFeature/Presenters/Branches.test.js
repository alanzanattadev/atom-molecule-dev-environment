import React from 'react';
import { shallow, mount } from 'enzyme';
import Branches from './Branches';
import Branch from './Branch';

describe('Branches', () => {
  it('should display many branches', () => {
    let subject = ;

  });
});
