"use babel";
// @flow

export type Branch = {
  name: string,
  current: boolean,
  remote: ?Remote,
};

export type Remote = {
  name: string,
};

export type CheckoutBranchAction = (dispatch: (action: any) => void) => void;
